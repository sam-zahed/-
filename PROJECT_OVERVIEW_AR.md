# شرح المشروع - نظام مساعدة المكفوفين بالذكاء الاصطناعي

## 📋 نظرة عامة

هذا المشروع عبارة عن **نظام ذكي لمساعدة المكفوفين وضعاف البصر** باستخدام الذكاء الاصطناعي. يعمل النظام على تحليل الصور والأصوات في الوقت الفعلي لمساعدة المستخدم على التنقل والتعرف على الأشياء والأشخاص من حوله.

## 🏗️ معمارية المشروع

### المكونات الرئيسية:

```
fastapi_agent_full_package/
├── app/                          # التطبيق الرئيسي
│   ├── main.py                   # نقطة البداية - FastAPI
│   ├── database.py               # اتصال قاعدة البيانات
│   ├── models.py                 # نماذج البيانات (Events, Maps, etc.)
│   │
│   ├── vision/                   # 👁️ الرؤية الحاسوبية
│   │   ├── model.py             # YOLO-World للكشف عن الأشياء
│   │   ├── router.py            # API endpoints للرؤية
│   │   └── ttc.py               # حساب وقت الاصطدام
│   │
│   ├── audio/                    # 🎤 معالجة الصوت
│   │   ├── transcribe.py        # تحويل الصوت لنص (Whisper)
│   │   ├── tts.py               # تحويل النص لصوت
│   │   └── router.py            # API endpoints للصوت
│   │
│   ├── infer/                    # 🧠 الاستنتاج الذكي
│   │   └── router.py            # معالجة الصور في الوقت الفعلي
│   │
│   ├── interactive/              # 🤝 التعلم التفاعلي
│   │   └── router.py            # تعلم الوجوه والأسماء
│   │
│   ├── events/                   # 📊 تسجيل الأحداث
│   │   └── router.py            # حفظ البيانات والقرارات
│   │
│   ├── base_map/                 # 🗺️ الخريطة الأساسية
│   │   └── router.py            # حفظ المعالم الثابتة
│   │
│   ├── change_queue/             # 🔄 قائمة التغييرات
│   │   └── router.py            # اكتشاف التغييرات في البيئة
│   │
│   └── device_link/              # 📱 الاتصال بالأجهزة
│       └── router.py            # WebSocket للاتصال المباشر
│
├── docker-compose.yml            # تشغيل جميع الخدمات
├── requirements.txt              # المكتبات المطلوبة
└── n8n_templates/                # قوالب أتمتة العمليات
```

## 🔧 كيف يعمل النظام؟

### 1️⃣ **الرؤية الحاسوبية (Vision Module)**

**الملف الرئيسي:** `app/vision/model.py`

- يستخدم نموذج **YOLO-World v2** - وهو نموذج متقدم يمكنه التعرف على **أي شيء** تحدده له!
- يحتوي على قاموس مخصص بـ **60+ كلمة** مهمة للمكفوفين:
  - **العوائق:** باب، درج، حفرة، جدار، زاوية
  - **الأشخاص:** شخص، طفل، رجل، امرأة
  - **المركبات:** سيارة، شاحنة، باص، دراجة
  - **الأثاث:** كرسي، طاولة، سرير، كنبة
  - **الأشياء الصغيرة:** مفاتيح، محفظة، جوال، كوب

**المميزات:**
- ✅ **ترجمة تلقائية للعربية** - كل شيء يُكتشف يُترجم فوراً
- ✅ **تقدير المسافة** - يحسب بُعد الأشياء عن المستخدم
- ✅ **التعرف على الوجوه** - يتعلم أسماء الأشخاص ويتذكرهم

**مثال على الاستخدام:**
```python
# النظام يرى صورة ويعيد:
{
  "class": "door",           # الشيء المكتشف
  "class_ar": "باب",         # الترجمة العربية
  "conf": 0.85,              # نسبة الثقة (85%)
  "distance_m": 2.0          # المسافة: 2 متر
}
```

### 2️⃣ **معالجة الصوت (Audio Module)**

**الملفات:** `app/audio/transcribe.py` + `app/audio/tts.py`

- **ASR (تحويل الصوت لنص):** يستخدم **Whisper** من OpenAI
- **TTS (تحويل النص لصوت):** يحول الأوامر النصية لصوت

**الاستخدام:**
- المستخدم يتكلم → النظام يفهم الكلام
- النظام يرد بالصوت → المستخدم يسمع الإجابة

### 3️⃣ **التعلم التفاعلي (Interactive Module)**

**الملف:** `app/interactive/router.py`

**السيناريو:**
1. المستخدم يلتقط صورة لشخص
2. يقول بصوته: "هذا أحمد" أو "This is Ahmed"
3. النظام يتعلم وجه أحمد ويحفظه
4. في المرة القادمة، يقول: "أحمد أمامك"

**الذكاء:**
- يفهم العربية والإنجليزية
- يستخرج الاسم من الجملة تلقائياً
- يحفظ الوجه في `app/data/faces/`

### 4️⃣ **الاستنتاج في الوقت الفعلي (Infer Module)**

**الملف:** `app/infer/router.py`

**الوظيفة الأساسية:** `/infer/realtime`

هذا هو **قلب النظام** - يستقبل صورة ويعيد تحليل كامل:

```json
{
  "objects": [
    {
      "class": "door",
      "confidence": 0.85,
      "distance_m": 2.0,
      "bbox": [100, 150, 300, 450]
    }
  ],
  "TTC": 999.0,                    // وقت الاصطدام
  "scene_confidence": 0.8,         // ثقة التحليل
  "inference_time_ms": 150         // وقت المعالجة
}
```

### 5️⃣ **قاعدة البيانات (Database)**

**الملف:** `app/models.py`

**الجداول:**

1. **Events** - تسجيل كل حدث (صورة، قرار، وقت)
2. **MapFeatures** - الخريطة الثابتة (معالم دائمة)
3. **ChangeQueue** - التغييرات المؤقتة (باب مفتوح/مغلق)
4. **LabelingTasks** - مهام التصنيف اليدوي

### 6️⃣ **n8n - أتمتة العمليات**

**المجلد:** `n8n_templates/`

n8n هو نظام أتمتة يربط كل شيء معاً:

- **Ingest-Realtime-Decision.json** - معالجة الصور الفورية
- **ChangeQueueProcessor.json** - معالجة التغييرات
- **LabelingQueue.json** - إرسال البيانات للتصنيف

## 🚀 كيفية الاستخدام

### التشغيل بـ Docker (الطريقة الموصى بها):

```bash
# 1. تشغيل جميع الخدمات
docker compose up --build

# 2. الخدمات ستعمل على:
# - FastAPI: http://localhost:8000
# - n8n: http://localhost:5678
# - PostgreSQL: localhost:5432
# - MinIO: http://localhost:9000
```

### تحميل النماذج:

```bash
# تحميل YOLO-World + Whisper
./download_models.sh
```

### اختبار API:

```bash
# 1. اختبار الكشف عن الأشياء
curl -X POST http://localhost:8000/vision/detect \
  -F "file=@image.jpg"

# 2. اختبار تحويل الصوت لنص
curl -X POST http://localhost:8000/audio/asr \
  -F "file=@audio.mp3"

# 3. الاستنتاج في الوقت الفعلي
curl -X POST http://localhost:8000/infer/realtime \
  -H "Content-Type: application/json" \
  -d '{
    "event_id": "test123",
    "user_id": "user1",
    "small_image_b64": "base64_encoded_image_here"
  }'
```

## 💡 تطويرات مقترحة لمساعدة المكفوفين

### 🎯 **تطويرات قصيرة المدى (شهر واحد)**

#### 1. **نظام التنبيه الصوتي الذكي**
```python
# إضافة أولويات للتنبيهات
PRIORITY_ALERTS = {
    "stairs": "CRITICAL",      # درج - خطر فوري
    "hole": "CRITICAL",        # حفرة - خطر فوري
    "car": "HIGH",             # سيارة - تحذير عالي
    "person": "MEDIUM",        # شخص - متوسط
    "chair": "LOW"             # كرسي - منخفض
}

# التنبيه الصوتي:
# "تحذير! درج على بعد مترين أمامك"
# "انتبه! سيارة على يمينك"
```

**الملف للتعديل:** `app/infer/router.py`

#### 2. **وضع الملاحة الداخلية**
- حفظ خريطة للأماكن المألوفة (البيت، المكتب)
- التعرف على الغرف تلقائياً
- إرشادات صوتية: "أنت في المطبخ، الثلاجة على يسارك"

**الملف للإضافة:** `app/navigation/indoor_nav.py`

#### 3. **البحث عن الأشياء المفقودة**
```python
# المستخدم يقول: "أين مفاتيحي؟"
# النظام يبحث في آخر 100 صورة
# يرد: "شاهدتها آخر مرة على الطاولة قبل 10 دقائق"
```

**الملف للإضافة:** `app/search/object_finder.py`

### 🚀 **تطويرات متوسطة المدى (3-6 أشهر)**

#### 4. **التعرف على النصوص (OCR)**
```python
# إضافة EasyOCR أو PaddleOCR
# قراءة:
# - اللافتات في الشوارع
# - أسماء المحلات
# - تواريخ الصلاحية على المنتجات
# - أرقام الحافلات
```

**المكتبة:** `easyocr` أو `paddleocr`

#### 5. **نظام الإضاءة الذكي**
```python
# كشف مستوى الإضاءة
# تحذير: "الإضاءة ضعيفة، استخدم الفلاش"
# تحسين الصور تلقائياً في الظلام
```

#### 6. **التعرف على العملات والنقود**
```python
# تدريب نموذج خاص بالعملات المحلية
# "هذه ورقة 100 جنيه"
# "هذه عملة معدنية 5 جنيه"
```

#### 7. **وضع التسوق**
```python
# مسح الباركود
# قراءة السعر
# التعرف على المنتجات
# "هذا حليب كامل الدسم، السعر 25 جنيه"
```

### 🌟 **تطويرات طويلة المدى (6-12 شهر)**

#### 8. **الملاحة الخارجية بـ GPS**
```python
# دمج مع Google Maps
# إرشادات صوتية خطوة بخطوة
# كشف إشارات المرور
# تحذير من المركبات القادمة
```

#### 9. **الذكاء الاصطناعي التحادثي**
```python
# دمج مع GPT-4 أو Claude
# المستخدم: "ماذا أمامي؟"
# النظام: "أمامك باب خشبي مغلق، وعلى يمينك نافذة كبيرة"
# المستخدم: "كيف أفتح الباب؟"
# النظام: "المقبض على اليسار، أدره عكس عقارب الساعة"
```

#### 10. **التعلم المستمر**
```python
# النظام يتعلم من سلوك المستخدم
# يتذكر الأماكن المفضلة
# يتوقع الاحتياجات
# "عادة تشرب قهوة الآن، الكوب على الطاولة"
```

#### 11. **مجتمع المستخدمين**
```python
# مشاركة الخرائط بين المستخدمين
# تحذيرات جماعية: "حفرة جديدة في شارع كذا"
# تقييم الأماكن: "هذا المطعم صديق للمكفوفين"
```

## 🔧 كيفية إضافة ميزة جديدة

### مثال: إضافة كشف الألوان

#### 1. إنشاء ملف جديد: `app/vision/color_detector.py`

```python
import cv2
import numpy as np

COLOR_NAMES_AR = {
    'red': 'أحمر',
    'blue': 'أزرق',
    'green': 'أخضر',
    'yellow': 'أصفر',
    'white': 'أبيض',
    'black': 'أسود'
}

def detect_dominant_color(image_bytes):
    """كشف اللون السائد في الصورة"""
    arr = np.frombuffer(image_bytes, np.uint8)
    img = cv2.imdecode(arr, cv2.IMREAD_COLOR)
    
    # تحويل لـ HSV
    hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)
    
    # حساب اللون السائد
    # ... منطق الكشف ...
    
    return {
        'color': 'red',
        'color_ar': 'أحمر',
        'confidence': 0.85
    }
```

#### 2. إضافة endpoint في `app/vision/router.py`

```python
from .color_detector import detect_dominant_color

@router.post('/detect_color')
async def detect_color(file: UploadFile = File(...)):
    content = await file.read()
    result = detect_dominant_color(content)
    return result
```

#### 3. تحديث `app/main.py` (إذا لزم الأمر)

```python
# الـ router موجود بالفعل، لا حاجة لتعديل
```

## 📱 التكامل مع تطبيق موبايل

### الخطوات:

1. **تطبيق Android/iOS** يلتقط الصور
2. يرسلها لـ `/infer/realtime`
3. يستقبل النتائج
4. يحولها لصوت ويشغلها للمستخدم

### مثال كود Flutter:

```dart
// التقاط صورة
final image = await ImagePicker().pickImage(source: ImageSource.camera);
final bytes = await image.readAsBytes();
final base64Image = base64Encode(bytes);

// إرسال للـ API
final response = await http.post(
  Uri.parse('http://your-server:8000/infer/realtime'),
  body: jsonEncode({
    'event_id': uuid.v4(),
    'user_id': 'user123',
    'small_image_b64': base64Image,
  }),
);

// معالجة النتيجة
final result = jsonDecode(response.body);
for (var obj in result['objects']) {
  speak('${obj['class_ar']} على بعد ${obj['distance_m']} متر');
}
```

## 🎓 نصائح للتطوير

### 1. **اختبر كل ميزة بشكل منفصل**
```bash
# اختبار الرؤية فقط
curl -X POST http://localhost:8000/vision/detect -F "file=@test.jpg"

# اختبار الصوت فقط
curl -X POST http://localhost:8000/audio/asr -F "file=@test.mp3"
```

### 2. **راقب الأداء**
```python
# إضافة logging في كل endpoint
import time
start = time.time()
# ... الكود ...
print(f"⏱️ Execution time: {time.time() - start:.2f}s")
```

### 3. **استخدم البيانات الحقيقية**
- اجمع صور من بيئات مختلفة
- سجل أصوات بلهجات مختلفة
- اختبر في ظروف إضاءة متنوعة

### 4. **التحسين المستمر**
```python
# حفظ الأخطاء للتحليل
if confidence < 0.5:
    save_for_review(image, prediction)
```

## 📊 مقاييس النجاح

- **دقة الكشف:** > 85% للأشياء الشائعة
- **سرعة الاستجابة:** < 500ms للصورة الواحدة
- **التعرف على الوجوه:** > 90% للأشخاص المحفوظين
- **فهم الصوت:** > 80% للكلام الواضح

## 🤝 المساهمة

لإضافة ميزة جديدة:
1. أنشئ branch جديد
2. أضف الكود في المجلد المناسب
3. اكتب tests
4. أرسل Pull Request

## 📞 الدعم

للأسئلة والمساعدة:
- افتح Issue على GitHub
- راجع ملفات `docs/` للتفاصيل التقنية
- اقرأ `n8n_templates/` لفهم سير العمل

---

**ملاحظة مهمة:** هذا المشروع في مرحلة التطوير. الميزات الأساسية تعمل، لكن يحتاج لتحسينات كثيرة ليكون جاهزاً للاستخدام اليومي من قبل المكفوفين.

**الأولوية الأولى:** التأكد من دقة وسرعة الكشف، لأن حياة المستخدم تعتمد على ذلك!
